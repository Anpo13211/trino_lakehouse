version: '3.8'

services:
  trino-worker:
    image: trinodb/trino:latest
    container_name: trino-worker-12
    hostname: trino-worker-12
    network_mode: host
    environment:
      - TRINO_ENVIRONMENT=production
    volumes:
      - ./config:/etc/trino
      - trino-worker-12-data:/var/trino/data
    deploy:
      resources:
        limits:
          memory: 50G
        reservations:
          memory: 50G
    restart: unless-stopped

volumes:
  trino-worker-12-data:
